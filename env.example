# Copy this file to .env for local development.
# On Render, DATABASE_URL is injected automatically by render.yaml once you
# attach the critter-dash-db database (or set it manually in the dashboard).

DATABASE_URL=postgresql://user:password@localhost:5432/critterdash
PORT=3000
